export type Size = "s" | "m" | "l" | "xl";

export enum IGameType {
  TUTORIAL = "TUTORIAL",
  SOLO = "SOLO",
  RT_SOLO = "RT_SOLO",

  SINGLE = "SINGLE",
  RT_SINGLE = "RT_SINGLE",

  RATING = "RATING",
  TEAM = "TEAM",
}

export enum ETutorialType {
  THEORY = "theory",
  PRACTICE = "practice",
}

export enum GameSide {
  NATO = "NATO",
  RUSSIA = "RUSSIA",
}
